[project]
name = "quality-seed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ashrith Desai", email = "314579464+asrithdesai@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
quality-seed = "quality_seed:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pre-commit>=4.6.1",
    "pytest>=9.1.1",
    "ruff>=0.16.2",
]
